import { MpTrackDirective } from './mp-track.directive';

describe('MpTrackDirective', () => {
  it('should create an instance', () => {
    const directive = new MpTrackDirective();
    expect(directive).toBeTruthy();
  });
});
